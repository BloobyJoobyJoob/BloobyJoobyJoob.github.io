for (let i = 0; i < array.length; i++) {
  console.log[i]
}

array.forEach((element) => {
  console.log(element)
})

let i = 0
while (i < array.length) {
  i++
  console.log(array[i])
}

let element
element = document.getElementById('box')

const carData = {
  type: 'Fiat',
  model: '500',
  color: 'white',
}

let carColor = carData.color

let alphabet = ['a', 'b', 'c', 'd', 'e', 'f', 'g', 'h', 'i' /*etc......*/]

let letter = alphabet[5] // Get the letter at the 5 position in the array (e)
